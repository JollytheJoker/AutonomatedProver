from MObject import ElementrySet, Quantor


# GENERAL GLOBAL DEFINITIONS
definitions = {
    "integers": ElementrySet(quantor=Quantor.DEFINE, association='N'),
    "reels": ElementrySet(quantor=Quantor.DEFINE, association='R')
}